[project]
name = "l22hw"
version = "0.1.0"
requires-python = ">=3.10"

[tool.poetry]
package-mode = false

[project.dependencies]
dependencies = [
    "streamlit>=1.30",
    "pandas>=2.0",
    "requests>=2.31",
]